@page
@model LibraryProject.Pages.BookReturns.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BookReturn[0].ReturnDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BookReturn[0].SysDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BookReturn[0].bookRent)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.BookReturn) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ReturnDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SysDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.bookRent.Id)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
